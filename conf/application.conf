# Play basics (already in your file — keep the rest as-is)
play.filters.hosts.allowed = ["."]
play.i18n.langs = [ "en" ]

# NewsAPI settings
newsapi {
  baseUrl = "https://newsapi.org/v2"
  # Read from environment variable NEWSAPI_KEY at runtime (safer than hardcoding)
  key = "197e644898e24b5384081402fdaafcd3"
}

play.modules.enabled += "modules.Module"
